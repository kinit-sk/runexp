build_order:
    - run_args

run_args:
  project_name: disai-multimodal
  experiment_class: ${method:runexp.DummyExperiment}
  logger_class:
    _partial_: true
    _target_: runexp.MLFlowLogger
    tracking_uri: sqlite:///mlruns/mlruns.db
   